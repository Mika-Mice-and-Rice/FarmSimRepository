//if (mouse_check_button_pressed(mb_middle)) { // Assuming middle mouse button for switching
    //var target_crop = instance_nearest(mouse_x, mouse_y, obj_crop);
    //if (target_crop) {
        // Change the crop type of the target crop
       // if (target_crop.crop_type == "carrot") {
         //   target_crop.crop_type = "tomato"; // Change to tomato or any other crop type
       // } else {
           // target_crop.crop_type = "carrot"; // Change to carrot or any other crop type
       // }
        // Update the sprite of the crop based on the new crop type and growth stage
       // var crop_sprites = ds_map_find_value(crop_data, target_crop.crop_type);
      //  target_crop.sprite_index = crop_sprites[target_crop.growth_stage];
  //  }
//}

